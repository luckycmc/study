1.系统调用导致的协成切换?
因为程序运行时，它调用了系统调用函数read/write/recv/send.... 导致进入了系统内核，
系统内核产生了中断，从而让CPU获取控制权，系统会根据调度需求判断当前 进程的 系统调用函数是否有返回，
如果当前的系统调用如recv网卡还在接收中，则会运行系统上其它的进程，从而实现进程切换

但当用协程时，执行recv时会被用户写的协程调度器首先拦截，协程HOOK技术